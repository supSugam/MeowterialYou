[Low Priority]
- catting a code file still has non-contrasting colors for some text especially in light modes gnome-terminal

theme notification popup
theme media player in quick settings of quick settings tweaks
grayscale like but with our colors for app icons.folders etc

widget: light mode, weather next hours, country flag, now playing

chrome gtk4 transparent context menus and search suggestions etc, highlight color text,website invisible tab icon

overview app launcher texts, themed icons, themed dtp 


[Other Bugs]

[Quick Settings Tweaks]
- WIFI/BLUETOOTH LIST POPUP ON QUICK SETTINGS PANEL EXCEEDS THE SCREEN FROM TOP

good job, now when i use that chevron button to expand or collapse the text content, it has fixed height currently in our theme, the widget has fixed height  no matter the content is shown or hidden instead od adjusting its height like "auto"

see here:
.events-button:hover,
.world-clocks-button:hover,
.weather-button:hover,
.QSTWEAKS-media .media-message:hover,
.weather-button.message:hover,
.QSTWEAKS-weather:hover {
  background-color: rgba(0, 0, 0, 0.15);
  transition-duration: 100ms;
}

we are using black background with low transparency, i want to use it of our own color but similar effect



Format: [Component] [State] [Adwainta-default] [MeowterialYou]

Color Issues in Dark Mode:

1. [quick-toggle button, quick-toggle-icon etc buttons, many other components like almost everything with a background like notification message, weather card, world clock card in quick settings and calendar area, buttons etc all] [hover] [just brightens up subtly, the text remains lighter, the bg color remains that primary orange, so only thing that change is the component brightens up very subtly on hover] [ours do this opposite, it darkens up or changes the bg color to really dark making a drastic change which is not subtle and very bad for user experience]

2. [othe]




<!-- System dialog close button (poweroff) -->
<!-- Transparent topbar totally but with hover effects -->
<!-- add option to reapply in interactive -->




<!-- DND gap fixed by JustPerfection -->

we have a problem with QSTWEAKS-media in quick settings, the problem is that message title or body is sometimes long so it does not come up in same line so the expand button truncates or shows it all, so the problem is that when i use that expand button to show or expand the message content, it does its job but it does ot adjusts its component height relatively
before you make a fix let me tell you that if i uninstall theme and default to adwaita, the qstweaks message media DOES adjusts its height accordingly just fine so the issue is seem to be our theming css, for yaru's css see 
Yaru-gnome-shell.css
 

for quick settings tree see 
quick_settings_tree.json
 


 # Future Features & Technical Debt

## App Icon Theming

**Goal**: Dynamically tint standard app icons (in Dash/App Grid) to match the Material You Primary color, similar to Android 12+.

**Status**: Attempted and Failed (Jan 2026).

**Implementation Tried**:
- **CSS Filters**: Using `filter: grayscale(100%) sepia(100%) hue-rotate(...) saturate(...)`.
- **Targeting**: Successful selectors found (`.overview-icon`, `StIcon`). Tested with visual debug (Red borders, Cyan backgrounds).
- **Result**: Visual debug properties (`border`, `background`) render correctly, proving the selectors work. However, `filter` and `opacity` properties designed to affect the icon texture itself are **ignored** by the rendering engine on the test environment (GNOME Shell + Dash to Panel + specific graphics drivers).
- **Dash to Panel**: DTP achieves grayscale via `Clutter.DesaturateEffect` in JavaScript, which bypasses CSS limitations.

**Future Solution**:
- Cannot be solved via pure CSS Theme.
- requires a **GNOME Shell Extension** helper that injects Clutter Effects (or Shader Effects) onto the app icon actors, exposing parameters that the CSS theme can then control (or controlling them via GSettings).
- Alternatively, wait for GNOME Shell / St Engine updates that might expose better CSS filter support for these widgets.
